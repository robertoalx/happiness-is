doctype html
html(lang="en")
  head
    -
      const {title, description} =data.happinessis.meta
    title=title
    meta(name="description" content=description)
  body
    include _component/parallax/parallax

if config.entry.css.external
  link(rel="stylesheet", href=`${baseUrl}/style.css`)
